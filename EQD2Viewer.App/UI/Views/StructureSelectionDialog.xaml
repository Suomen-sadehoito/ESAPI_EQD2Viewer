<Window x:Class="EQD2Viewer.App.UI.Views.StructureSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Structures for DVH Analysis" 
        Height="500" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="#0C0E12" Foreground="#F0F2F5" FontFamily="Segoe UI">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/EQD2Viewer.App;component/UI/Styles/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select Structures" FontSize="15" FontWeight="SemiBold" Foreground="#F0F2F5" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="Choose structures for DVH analysis and EQD2 calculation." Foreground="#5C6475" FontSize="11" Margin="0,0,0,12"/>

        <ListBox Grid.Row="2" x:Name="StructureListBox" SelectionMode="Multiple" Background="#12151B" BorderBrush="#2A3040" BorderThickness="1" Padding="4">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Foreground="#F0F2F5" Margin="4,3" FontSize="11"
                              IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}">
                        <CheckBox.Content><TextBlock Text="{Binding Id}"/></CheckBox.Content>
                    </CheckBox>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#F0F2F5"/>
                    <Setter Property="Padding" Value="2"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Add selected" Style="{StaticResource BtnPrimary}" Width="110" Click="OK_Click" IsDefault="True" Margin="0,0,8,0"/>
            <Button Content="Cancel" Style="{StaticResource Btn}" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
